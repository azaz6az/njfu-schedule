# 南林课程表（kechengbioa）

Android 课程表 App：自动导入南林 / 广西大学教务课表 + 手动导入兜底，桌面小组件，课前提醒。

## 功能

- **自动导入**：南林（统一身份认证 CAS）与广西大学（正方 jwglxt 新版教务）双学校，WebView 登录 + 会话 Cookie 抓取课表与考试
- **手动导入兜底**：JSON / ICS / Excel 三种格式（教务系统改版或遇验证码时使用）
- **桌面小组件**：今日课程 2×2、下一节课 4×1（实时倒计时）、今日列表 4×2、考试倒计时 2×2、本周网格 4×3，五款可选；3 套主题（莫兰迪/清新/深邃）深浅色自适应；自适应刷新调度
- **小组件引导**：设置页内置「桌面小组件」区块——通用三步 + 按品牌查看 + 常见问题
- **桌面小组件**：2×2 今日课程摘要 + 4×3 本周网格（Glance），每日自动刷新，可在设置页手动刷新
- **周课表**：周次滑动切换、彩色课程卡片、今日高亮、空白格点击加课、课程详情编辑、调休日按映射星期显示
- **考试安排**：列表展示，临近 7 天高亮；考试提醒通知（提前 1/2/3/7 天 + 考试当天，可开关）
- **课前提醒**：提前 5/10/15 分钟通知（AlarmManager），开机自动重排，支持调休日映射
- **导入预览**：导入前展示与现有课表的差异（新增/删除/变更/不变 + 冲突提示），确认后才落库
- **课程冲突检测**：手动加课与现有课程时间重叠时先确认再添加
- **调休设置**：节假日调休日按周几显示的映射（如国庆后周六补周一的课），课表与提醒同时生效
- **设置**：学期起始日期、节次时间段、提醒提前量、考试提醒、调休映射、JSON 备份导出/导入、小米设备优化引导

## 版本历史

### v0.5.1（2026-08-16）

- **提醒可靠性**：修复打开 App 即误报「即将上课」（触发时刻已过无守卫）；Receiver 收到广播先查库校验课程仍存在且当天确应上课，删除/清空/导入课程后不再误报；排程扩展为「今天 + 明天 + Receiver 自续」，连续数天不打开 App 提醒也不断；Android 12+ 未授予「闹钟与提醒」时自动降级 setWindow 而非静默失效，设置页新增权限引导横幅
- **ICS 导入正确性**：带 `Z` / `±HHMM` 时区的课程时间换算到本地（此前整天错 8 小时）；周次按学期起始日定位，不再硬编码从第 1 周起算
- **数据安全**：导入替换改为原子事务（中途失败不再清空课表、小组件不再闪空）；登录凭据解密失败静默降级为未登录（覆盖卸载重装 / 换机恢复密文场景）；寒假期间学期自动推导修复（不再误显示「第 1 周」）
- **导入健壮性**：Excel 支持中文星期、非法行按行号记录并跳过、窄表不再越界崩溃；登录失败判定精准化（不再把任意含「用户名」的响应误报为密码错误）；WebView 登录页增加超时守卫与加载失败提示、修复泄漏并收紧安全设置
- **UI 修复**：停留课表页跨过周一零点后「本周」高亮自动刷新；文件解析移出主线程（避免大文件 ANR）；加课弹窗旋转不再丢编辑内容；上一周/下一周按钮提升至 48dp 触控目标；星期下拉补充无障碍语义
- **小组件**：本周网格 180dp 高度与 3 格目标对齐；下一节课倒计时窗口内改用容忍窗口闹钟降低功耗；无实例时统一短路不再空跑
- **工程化**：release 开启 R8 混淆 + 资源压缩（APK 14.4MB → 2.9MB）；POI 升级 5.5.1；Room schema 导出（app/schemas/1.json）+ 迁移策略注释；云端备份排除课表库与登录凭据、换机迁移保留课表；CI 增加 release 构建验证
- **本地化**：全部 234 条 UI 文案抽入 `strings.xml`（含小组件 / 通知 / 引导），为多语言铺路
- **测试**：新增提醒核心（17）、学期推导（7）、数据库事务（7）、小组件调度边界、解析器边界等用例，213 个单元测试全绿
- **架构**：versionCode 9 / versionName 0.5.1

### v0.5.0（2026-08-14）

- **广西大学导入**：新增正方 jwglxt 新版教务适配（jwxt2018.gxu.edu.cn）——WebView 登录（验证码/滑块在页面内完成）→ 课表 + 考试 JSON 接口抓取；导入向导支持学校切换与学年学期选择（默认按学期起始日期自动推导）
- **小组件矩阵扩展**：新增 4×1 下一节课（实时倒计时：距上课/距下课/今日无课）、4×2 今日列表、2×2 考试倒计时；2×2 今日课程与 4×3 本周网格加表头并应用主题；考试小组件点按直达考试页
- **小组件主题**：设置页三选一（莫兰迪纸感/清新浅色/深邃夜间），深浅色自适应渲染
- **自适应刷新**：4×1 倒计时在上课窗口内每分钟刷新（AlarmManager 一次性闹钟，窗口外仅上课前 60 分钟/次日 08:00/数据变更时刷新），无实例自动停表
- **小组件引导**：设置页「桌面小组件」区块——通用三步 + 7 家品牌差异 + 常见问题
- **架构**：CasLoginActivity 通用化（学校参数化）；SchoolAdapter 多学校（School 枚举）；versionCode 8 / versionName 0.5.0
- **测试**：GxuParser 课表/考试 fixture、学年学期推导、倒计时状态机、主题色板对比度、引导文案完整性等新增用例

### v0.4.0（2026-08-14）

- **考试提醒**：新增强制提醒通知——提前 1/2/3/7 天 09:00 + 考试当天 08:00 各提醒一次；设置页可开关与调提前量；删除考试自动取消对应提醒
- **导入预览 + 差异合并**：所有导入（自动/JSON/ICS/Excel/xls）解析后先与现有课表对比（新增/删除/变更/不变），展示差异与时间冲突后才确认落库，避免误覆盖
- **调休设置**：把某天映射为「按周几显示」（1-7）或「放假」（0），课表网格列、今日高亮、FAB 加课默认星期、课前提醒全部按映射生效；设置页可增删映射
- **课程冲突检测**：加课时与现有课程同日、同周次、节次重叠时弹确认框；导入预览中提示新数据内部冲突数量
- **架构**：迁移 Gradle Version Catalog（gradle/libs.versions.toml），versionCode 4 / versionName 0.4.0
- **测试**：新增 Compose UI 测试（Robolectric 在 JVM 运行 CourseDialog 交互）、小组件数据逻辑测试、调休解析/网格映射/冲突检测/导入差异等 35 个用例，共 104 个单元测试全绿
- **体验**：应用图标（莫兰迪自适应图标）、Splash Screen 启动页、Android 12+ Material You 动态取色（莫兰迪回退）
- **小米/MIUI 适配**：设置页新增「小米设备优化」区块——一键跳转自启动管理/省电策略设置、手动刷新小组件
- **考试列表**：点击考试卡片可删除（自动取消对应提醒）

### v0.3.0（2026-08-14）

- **修复卡片全透明（白底无字）的根因**：CourseMapper 用 Color.value.toInt() 提取 ARGB，但 Compose Color.value 编码为 0xAARRGGBB00000000（ARGB 在高 32 位），.toInt() 取低 32 位恒为 0 → 所有课程卡颜色为 0 = 全透明，露出白色网格底、白字不可见。改为 (value shr 32).toInt() 后卡片恢复彩色、文字清晰（0.1.0 起就存在的真 bug，此前对比度修复无效的原因）
- 新增回归测试锁定色板提取与卡片不透明度（69 个单元测试全绿）

### v0.2.0（2026-08-12）

- **修复课程卡片可读性**：课程卡 6 色板加深为可读莫兰迪档，白字对比度由 1.8~2.2:1 提升至 5.1~6.2:1（WCAG AA ≥ 4.5:1），历史浅色数据自动映射到新色板同槽位；课程名 11sp→12sp、地点/节次 9sp→10sp，透明度提至 95%/92%
- **修复同格多课重叠**：同一日期间部分重叠（如 1-2 与 1-4 节）的课程不再互相遮挡，按贪心列分配并排显示；不冲突的课程复用同一列保持卡片宽度
- **日期范围显示年份**：跨年的周次显示「2026年12月28日 – 2027年1月3日」，消除寒假跨年歧义
- **今日高亮仅在当前周**：切换到其他周次时不再高亮今日列；今日日期每分钟刷新，跨零点自动更新
- **FAB 加课默认今天**：右下角「加课」默认落到今天（而非固定周一）

## 构建

环境要求：

- JDK 17+（本机已验证 JDK 21）
- Android SDK（`local.properties` 中 `sdk.dir` 指向 SDK 路径，如 `sdk.dir=D:/Android/Sdk`）

```bash
./gradlew assembleDebug          # 构建 APK
./gradlew :app:testDebugUnitTest # 运行单元测试
```

APK 输出：`app/build/outputs/apk/debug/app-debug.apk`，直接安装到手机即可（需允许未知来源）。

## 使用

1. 打开 App →「导入」Tab → 输入学号密码 → 开始导入
2. 桌面长按 → 添加小组件 → 选择「今日课程」「下一节课」「今日列表」「考试倒计时」或「本周课表」（设置页「桌面小组件」区块有按品牌的分步引导）
3. 设置页配置学期起始日期（影响周次计算）与提醒提前量
4. 提醒权限：Android 12+ 需在系统设置授予「闹钟与提醒」，Android 13+ 需通知权限

## 手动导入格式

- **JSON**：App 设置页「导出备份」产出的格式（`{"version":1,"courses":[...],"exams":[...]}`），也支持裸课程数组 `[...]`
- **ICS**：iCal 周重复事件（RRULE 带 BYDAY/COUNT），时间按默认节次表映射
- **Excel**（xlsx）：表头 + 数据行，列顺序：`课程名,教师,地点,星期(1-7),开始节,结束节,周次`；周次支持 `1-16`、`1,3,5`、`单周`、`双周`

## 南林教务对接说明

- **自动导入 = WebView 登录 + Cookie 桥接**：`CasLoginActivity` 用系统 Chrome 内核打开 CAS 登录页，用户完成登录（含验证码）后读取 jwxt 会话 Cookie（`bzb_jsxsd`），`NjfuAdapter.fetchScheduleWithCookies()` 带 Cookie 抓取 `xskb_list.do` 课表页。
  - 原因：实测 jwxt 反向代理会拒绝一切非浏览器客户端的 CAS ticket 落地（OkHttp/curl/httpx 均 404，仅真实浏览器通过），故登录必须走 WebView；带有效会话 Cookie 的普通页面请求则可正常访问。
  - 课表页结构（`<table id="timetable">`、大节行、kbcontent 变体 div、`-----` 分隔多课程块）已用真实页面 fixture 锁定，见 `JwxtParser`。
- **手动导入兜底**：推荐教务网页端「学生个人课表」导出的 `.xls`（`NjfuXlsImporter`，真实导出文件已验证）；另支持 JSON 备份 / ICS / Excel（xlsx）。
- **周次解析**：统一入口 `WeekUtils.parseWeeksText()`，支持 `1-16`、`1,3,5`、`1-12(周)[01-02节]`、`1-12([周])[01-02节]`、`1-16(单)`、`2-16(双)`、`单周` 等写法；解析失败按全学期显示并在导入结果中提示。
- 历史参考：`CasLoginClient`/`RsaEncryptor` 实现了表单登录协议（AES-128-CBC 复合加密，与线上 `encrypt.js` 一致），现因 WAF 拦截不作为主路径，保留供协议回归测试。

## 项目结构

```
app/src/main/java/com/schedule/njfu/
├── importer/          # SchoolAdapter 接口 / School 枚举 / NjfuAdapter + GxuAdapter（WebView 登录+JSON 课表/考试）/ JSON/ICS/Excel 导入
├── data/              # Room 实体/DAO/仓库/凭据 Keystore 加密
├── widget/            # RemoteViews 小组件（2×2 今日 / 4×1 下一节 / 4×2 今日列表 / 2×2 考试 / 4×3 周网格）+ 主题 + 自适应刷新调度
├── reminder/          # 课前提醒（AlarmManager + 开机重排）
└── ui/                # Compose：周课表/考试/设置（含小组件引导与主题）/导入向导 + 底部导航
```

设计文档：`docs/superpowers/specs/2026-08-12-njfu-schedule-widget-design.md`、`docs/superpowers/specs/2026-08-14-gxu-import-widgets-design.md`
实现计划：`docs/superpowers/plans/2026-08-12-njfu-schedule-widget.md`

## 已知限制

- 自动导入的课表页 URL 与接口字段尚未真机验证（南林 `jsxsd/xskb_list.do`、广西大学 `jwglxt/xskbcx_cxXsKb` 与考试接口 `kscx_cxXsksxxDg` 均为标准正方接口假设）
- 验证码/滑块场景由用户在 WebView 内手动完成（自动导入失败时走手动导入）
- 小组件刷新为每日一次（WorkManager）+ 4×1 倒计时分钟级闹钟（仅倒计时窗口内），打开 App 时也会刷新
- 提醒依赖系统「闹钟与提醒」授权，省电策略可能延迟通知