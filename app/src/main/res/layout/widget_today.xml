<?xml version="1.0" encoding="utf-8"?>
<!-- 4x2 今日课程小组件：表头行 + 课程列表 + 考试倒计时行 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="8dp">

    <TextView
        android:id="@+id/today_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:fontFamily="sans-serif-medium"
        android:maxLines="1"
        android:text="周X · 第N周 · N节课"
        android:textColor="#4A4A44"
        android:textSize="13sp" />

    <LinearLayout
        android:id="@+id/today_course_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical" />

    <TextView
        android:id="@+id/exam_line"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:background="@drawable/widget_exam_bg"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingLeft="8dp"
        android:paddingTop="3dp"
        android:paddingRight="8dp"
        android:paddingBottom="3dp"
        android:text="考试倒计时：课程名 · 还有X天"
        android:textColor="#4A4A44"
        android:textSize="11sp"
        android:visibility="gone" />
</LinearLayout>
