<resources>
    <!-- 启动页（Splash Screen API）：显示图标与背景后无缝切换到主主题 -->
    <style name="Theme.Schedule" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Schedule.Main</item>
    </style>

    <style name="Theme.Schedule.Main" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
